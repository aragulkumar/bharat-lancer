apiVersion: apps/v1
kind: Deployment
metadata:
  name: bharathlancers-frontend
  labels:
    app: bharathlancers
    component: frontend
spec:
  replicas: 2
  selector:
    matchLabels:
      app: bharathlancers
      component: frontend
  template:
    metadata:
      labels:
        app: bharathlancers
        component: frontend
    spec:
      containers:
      - name: frontend
        image: yourusername/bharathlancers-frontend:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 80
          protocol: TCP
        resources:
          requests:
            memory: "128Mi"
            cpu: "100m"
          limits:
            memory: "256Mi"
            cpu: "200m"
        livenessProbe:
          httpGet:
            path: /
            port: 80
          initialDelaySeconds: 10
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /
            port: 80
          initialDelaySeconds: 5
          periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: bharathlancers-frontend
  labels:
    app: bharathlancers
    component: frontend
spec:
  selector:
    app: bharathlancers
    component: frontend
  ports:
  - name: http
    protocol: TCP
    port: 80
    targetPort: 80
  type: ClusterIP
---
apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: bharathlancers-frontend
  labels:
    app: bharathlancers
    component: frontend
spec:
  to:
    kind: Service
    name: bharathlancers-frontend
    weight: 100
  port:
    targetPort: http
  tls:
    termination: edge
    insecureEdgeTerminationPolicy: Redirect
  wildcardPolicy: None
